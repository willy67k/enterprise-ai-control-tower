{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 7801",
    "main": "uv run app/main.py",
    "install": "uv sync",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format .",
    "test": "uv run pytest"
  }
}
